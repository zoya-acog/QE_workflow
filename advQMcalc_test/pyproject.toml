[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "advQMcalc_test"
version = "0.0.16"
description = "Repo skeleton"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[project.scripts]
advQMcalc_test = "advQMcalc_test.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
